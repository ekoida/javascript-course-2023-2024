{
  "name": "app",
  "version": "1.0.0",
  "description": "http-server",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "psql -U postgres"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "postgres": "^3.4.4"
  }
}
